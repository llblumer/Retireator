PROJECT=Retireator
VERSION=2015_0318
SUFFIX=dbg
EXT=xlsx
PULLTARGET=$(PROJECT)_$(VERSION)_$(SUFFIX).$(EXT)
PUSHTARGET=$(EXT)_$(SUFFIX)

pull: $(PULLTARGET)

push: $(PUSHTARGET)

$(PULLTARGET): 
	7za a  $@ .\$(PUSHTARGET)\*

$(PUSHTARGET): 
	7za x $(PULLTARGET) -o.\$@\ -y

