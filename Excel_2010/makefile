PROJECT=Retireator
VERSION=2015_0412
SUFFIX=
EXT=xlsm
PULLTARGET=$(PROJECT)_$(VERSION)$(SUFFIX).$(EXT)
PUSHTARGET=$(EXT)_repo

pull: $(PULLTARGET)

push: $(PUSHTARGET)

$(PULLTARGET): 
	7za a  $@ .\$(PUSHTARGET)\*

$(PUSHTARGET): 
	7za x $(PULLTARGET) -o.\$@\ -y

