PROJECT=Retireator
VERSION=2015_0517
SUFFIX=
EXT=xlsm
PULLTARGET=$(PROJECT)_$(VERSION)$(SUFFIX).$(EXT)
PUSHTARGET=$(EXT)_repo

pull: $(PULLTARGET)

push: $(PUSHTARGET)

$(PULLTARGET): 
	7za a -tzip $@ .\$(PUSHTARGET)\*

$(PUSHTARGET): 
	7za x -tzip $(PULLTARGET) -o.\$@\ -y

